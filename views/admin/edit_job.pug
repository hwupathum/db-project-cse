doctype html
html(lang='en')
    include ../common/head.pug
    +head("Starter MVP")
    body
        header
            include ../common/navbar.pug
            +navbar()
            .container
                .row
                    .col-sm-12.offset-md-2.col-md-8
                        br
                        .jumbotron
                            h2.text-center Edit Job
                            .row
                                .col-sm-12.col-md-12
                                    form(action='/admin/jobs/edit/'+formData.job_id, method='POST')
                                        div.formgroup
                                            label.small.mb-1.mt-3 Job Title:
                                            input(required='true', name='title', type='text', value=formData.title).form-control

                                            if (errors && errors.message)
                                                p.small.text-danger #{errors.message}
                                            button.mt-3.btn.btn-success(type='submit') Save
    include ../common/footer.pug
    +footer()