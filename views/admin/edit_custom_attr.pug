doctype html
html(lang='en')
    include ../admin_common/head.pug
    +head("Admin Portal")
    body
        header
            include ../admin_common/navbar.pug
            +navbar(admin)
            .container
                .row
                    .col-sm-12.offset-md-2.col-md-8
                        br
                        .jumbotron
                            h2.text-center Edit Custom Attributes
                            .row
                                .col-sm-12.col-md-12
                                    form(action='/admin/custom_attr/edit/' + formData.attr_id, method='POST')
                                        div.formgroup
                                            label.small.mb-1.mt-3 Attribute:
                                            input(required='true', name='attribute', type='text', value=formData.attribute).form-control

                                            if (errors && errors.message)
                                                p.small.text-danger #{errors.message}
                                            button.mt-3.btn.btn-success(type='submit') Save
    include ../admin_common/footer.pug
    +footer()